# =====================================================
# 流水线相关 Schema（已废弃）
#
# 创意工坊（WorkshopView）及流水线模板 CRUD 已下线，
# 项目制创作（Project）已迁移到 /api/projects/*。
#
# 风格预设 / 剧本模板 / 资产库 的 schema 定义在 app.schemas.assets，
# 项目相关 schema 定义在 app.schemas.project。
# 本文件保留为占位，不再定义任何 schema。
# =====================================================
