# 前端环境变量配置说明

本项目支持的环境变量（创建 `.env` 文件与本文件同级）。

```bash
# ============================================================
# 后端 API 基础地址（生产部署必需，开发模式留空即可）
# 留空时：使用相对路径 /api，请求由 Vite dev server 代理到 http://localhost:8000
# 生产部署时：填写你的后端服务域名（注意末尾不要加 /）
# ============================================================
# VITE_API_BASE_URL=

# ============================================================
# 网站标题（可选）
# ============================================================
# VITE_APP_TITLE=Agnes AI Platform
```

## 使用方式

```bash
# 1. 复制模板
cp .env.example .env

# 2. 根据需要修改 .env（开发模式下通常无需修改）

# 3. 启动项目
npm run dev
```

## 规则说明

- 所有**前端**使用的环境变量**必须以 `VITE_` 为前缀**，否则 Vite 不会在构建时注入
- `.env` 文件不要提交到 Git 仓库（已在 `.gitignore` 中排除）
